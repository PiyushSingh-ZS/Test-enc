kind: DaemonSet

serviceAccount:
  create: true
  annotations: {}
  name: fluent-bit
  namespace: monitoring

rbac:
  create: true
  nodeAccess: false

envFrom:
  - secretRef:
      name: fluent-bit-secrets
  - configMapRef:
      name: fluent-bit-config-map
